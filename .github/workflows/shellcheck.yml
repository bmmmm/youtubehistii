name: ShellCheck

on:
  push:
    branches: [main]
    paths: ['scripts/**']
  pull_request:
    branches: [main]
    paths: ['scripts/**']
  workflow_dispatch:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: './scripts'
          severity: warning
        env:
          SHELLCHECK_OPTS: --shell=bash --exclude=SC1090,SC1091,SC2034,SC1007
